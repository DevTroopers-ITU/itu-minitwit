name: Report

on:
  push:
    branches: [master, dev]
    paths: [report/**]
  workflow_dispatch:

jobs:
  build-pdf:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Pandoc and XeLaTeX
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y texlive-xetex texlive-fonts-recommended texlive-latex-extra texlive-lang-european
          sudo snap install pandoc --classic

      - name: Build PDF
        run: |
          mkdir -p report/build
          cd report
          pandoc report.md \
            -o build/MSc_group_q.pdf \
            --pdf-engine=xelatex \
            --resource-path=.

      - name: Commit PDF
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add report/build/MSc_group_q.pdf
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "docs: rebuild report PDF"
            git push
          fi
